import React, { useState } from "react";

const TodoList = () => {
  const [task, setTast] = useState("");
  const [tastList, setTastList] = useState([]);
  const handleChange = () => {
    // Unas cosillas
  };

  const handleAddTask = () => {
    // Unas cosillas
  };
  return (
    <div>
      <input />
      <ul>
        <li>Tarea</li>
      </ul>
    </div>
  );
};

export default TodoList;
